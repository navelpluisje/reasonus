<header class="home-header">
    <section class="background"></section>
    <section class="image">
        <img src="{{ '/images/logo.webp' }}" alt="" class="logo">
        <img src="{{ '/images/Faderport8-header-large.webp'}}" alt="" class="device"/>
    </section>
    <section class="content">
        <header class="title">{{ home.Title }}</header>
        <p>{{ home.Content | pathPrefix | safe }}</p>
        <a class="button" href="{{ home.ButtonUrl }}">{{ home.Button }}</a>
    </section>
</header>